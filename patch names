  -- parse of the incoming dump
  local result = parse_sysex(dump)
  if not result or type(result.data) ~= "table" then
    print("Could not read data")
    return
  end
  local data = result.data
  local pNames = {}
  local idx = 1
  for i = 1,8192,16 do
    pNames[idx] = get_patch_name(data,i) -- update patchName
    idx = idx+1
  end
  for i = 1,#pNames do
    print("patch name "..i.." "..pNames[i])
  end
  print("patch names parsed. Number of bytes: ".. #data)
  return (pNames)
end
